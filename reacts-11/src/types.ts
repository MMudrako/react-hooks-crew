// Define the shape of a single Hook object
export type Hook = {
    id: string;
    name: string;
    hook: string;
    role: string;
    traits: string[];
    usedFor: string;
    icon: string;
    featured: boolean;
};
